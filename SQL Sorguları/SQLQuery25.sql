CREATE FUNCTION FN_TESTMULTI 
(
	
)
RETURNS 
@VERILER TABLE 
(
    SKOD INT,
	AD VARCHAR(20),
	FIYAT DECIMAL(8,2),
	MIKTAR INT,
	TSATIS INT,
	TCARI INT
	
)
AS
BEGIN
    INSERT INTO @VERILER(SKOD, AD, FIYAT, MIKTAR, TSATIS, TCARI)
	SELECT STOKKOD, STOKAD, GUNCELFIYAT, MIKTAR, 0, 0 FROM STOKLAR 
	
	
	RETURN 
END
GO